
function sortByGenre(genre, podcastData, setPodcastData) {
    const position = genre.indexOf('_');
    const sortby = genre.slice(position+1);

    


    

    setPodcastData(filteredSeries);
}
export default sortByGenre
